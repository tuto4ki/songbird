class Bird {

  constructor (id, name, species, description, image, audio) {
    this.id = id;
    this.name = name;
    this.species = species;
    this.description = description;
    this.image = image;
    this.audio = audio;
  }

}

export default Bird;